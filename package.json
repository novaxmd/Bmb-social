{
  "name": "bmbtech-root",
  "version": "1.0.0",
  "private": true,
  "description": "Bmbtech - Telegram synced video/music downloader",
  "scripts": {
    "setup": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "concurrently \"npm run start --prefix backend\" \"npm run start --prefix frontend\"",
    "db:migrate": "npm run db:migrate --prefix backend",
    "db:generate": "npm run db:generate --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
